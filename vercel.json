{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ -d apps/web ]; then npm --prefix apps/web install && npm --prefix apps/web run build && rm -rf dist && cp -r apps/web/dist dist; else npm install && npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
